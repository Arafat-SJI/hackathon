## Security architecture

### Authentication

- Describe how users are authenticated (e.g., Supabase Auth, OAuth providers).
- Document session management and token handling.

### Authorization

- Explain how access control is enforced (role-based, attribute-based, row-level security, etc.).
- Link to any database RLS policies or authorization middleware.

### Data protection

- Note where data is encrypted in transit and at rest.
- Describe handling of sensitive data (PII, secrets).

### Threat modeling & hardening

- List key threats and mitigations (e.g., injection, XSS, CSRF).
- Reference any external security checks or monitoring in place.

Keep this document updated when security mechanisms change or new risks are identified.

